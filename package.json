{
	"name": "omp-legion",
	"version": "0.1.0",
	"description": "MoA-over-MoE orchestration with HOTL governance for Oh My Pi",
	"type": "module",
	"main": "src/index.ts",
	"files": ["src/", "agents/", "rules/", "skills/"],
	"omp": {
		"name": "legion",
		"version": "0.1.0",
		"description": "MoA-over-MoE orchestration with HOTL governance for Oh My Pi",
		"extensions": ["src/index.ts"],
		"settings": {
			"modelMap": {
				"type": "string",
				"description": "JSON object mapping roles to models, strategy, and ensemble size."
			},
			"defaultEnsembleSize": {
				"type": "number",
				"description": "Default number of expert samples per role."
			},
			"maxConcurrentExperts": {
				"type": "number",
				"description": "Max concurrent expert attempts across one dispatch, all tasks combined."
			},
			"verifyCommand": {
				"type": "string",
				"description": "Optional shell command (e.g. 'bun test') run against each code-mutating attempt's isolated branch for execution-grounded consensus. Off by default."
			},
			"decisionTimeoutMs": {
				"type": "number",
				"description": "How long a HOTL escalation waits for a human before auto-resolving to reject. Default 30 minutes."
			},
			"decomposer": {
				"type": "string",
				"description": "JSON object with ordered models and optional temperature ladder for sequential decomposition."
			},
			"decomposer.models": {
				"type": "string",
				"description": "JSON array of decomposer model selectors, tried sequentially."
			},
			"decomposer.temperatureLadder": {
				"type": "string",
				"description": "JSON array of temperatures used by sequential decomposer attempts."
			},
			"hotl.confidenceFloor": {
				"type": "number",
				"description": "Escalate when synthesis confidence falls below this value."
			},
			"hotl.disagreementThreshold": {
				"type": "number",
				"description": "Escalate when synthesis disagreement exceeds this value."
			},
			"hotl.costCeiling": {
				"type": "number",
				"description": "Escalate when the mean token cost per attempt exceeds this value (scale-invariant regardless of ensemble size)."
			},
			"hotl.failureRateCeiling": {
				"type": "number",
				"description": "Escalate when the fraction of attempts that failed/aborted outright exceeds this value, independent of confidence."
			},
			"embed.baseUrl": {
				"type": "string",
				"description": "Ollama embedding service base URL."
			},
			"embed.apiKey": {
				"type": "string",
				"secret": true,
				"description": "Optional Ollama embedding service API key."
			},
			"embed.model": {
				"type": "string",
				"description": "Ollama embedding model name."
			}
		}
	},
	"scripts": {
		"typecheck": "tsc --noEmit",
		"test": "bun test",
		"lint": "biome check --diagnostic-level=error .",
		"format": "biome format --write ."
	},
	"dependencies": {
		"zod": "4"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.8.3",
		"@oh-my-pi/pi-tui": "^16.4.4",
		"@types/bun": "^1.3.14",
		"@types/node": "^22.20.0",
		"typescript": "^5.7.0"
	},
	"peerDependencies": {
		"@oh-my-pi/pi-coding-agent": ">=16.0.0",
		"@oh-my-pi/pi-mnemopi": ">=16.0.0",
		"@oh-my-pi/pi-tui": ">=16.0.0",
		"@oh-my-pi/pi-utils": ">=16.0.0"
	},
	"engines": {
		"bun": ">=1.3.0"
	},
	"keywords": ["oh-my-pi", "omp", "omp-plugin", "moa", "moe", "governance"],
	"license": "MIT"
}
